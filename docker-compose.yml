services:
  web:
    image: nginx:alpine
    ports:
      - 3200:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./dist:/usr/share/nginx/html:ro
    restart: unless-stopped
networks: {}
